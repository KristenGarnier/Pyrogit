# react

To install dependencies:

```bash
bun install
```

To run:

```bash
bun dev
```

This project was created using `bun create tui`. [create-tui](https://git.new/create-tui) is the easiest way to get started with OpenTUI.

install on your machine : curl -fsSL https://raw.githubusercontent.com/KristenGarnier/Pyrogit/refs/heads/main/install.sh | sh
